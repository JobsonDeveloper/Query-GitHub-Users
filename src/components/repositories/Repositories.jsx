import React from "react";
import * as S from "../panel/WrapperPanel";

export const Repositories = () => {
    return (
        <S.WrapperPanel>
                Repositories
        </S.WrapperPanel>
    )
}